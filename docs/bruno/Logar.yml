info:
  name: Logar
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8000/api/login
  body:
    type: json
    data: |-
      {
        "email": "caua.lopes@email.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
