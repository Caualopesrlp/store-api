info:
  name: Registrar
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/api/register
  body:
    type: json
    data: |-
      {
        "name": "Cauã",
        "email": "caua.lopes@email.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
